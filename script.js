function togglePhase(id) {
    const phase = document.getElementById(id);
    phase.style.display = (phase.style.display === "block") ? "none" : "block";
}

function toggleExercise(id) {
    const exercise = document.getElementById(id);
    exercise.style.display = (exercise.style.display === "block") ? "none" : "block";
}
